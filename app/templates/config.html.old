<!-- app/templates/config.html -->

{% extends "base.html" %}

{% block title %}Configuración{% endblock %}

{% block content %}
    <h2>Configuración</h2>
    {% if form.errors %}
        <div class="alert alert-danger">
            <ul>
                {% for field, errors in form.errors.items() %}
                    {% for error in errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                {% endfor %}
            </ul>
        </div>
    {% endif %}
    <form method="POST">
        {{ form.hidden_tag() }}
        <div class="form-group">
            {{ form.color_primary.label }}
            {{ form.color_primary(class="form-control") }}
        </div>
        <div class="form-group">
            {{ form.color_secondary.label }}
            {{ form.color_secondary(class="form-control") }}
        </div>
        <div class="form-group">
            {{ form.color_tertiary.label }}
            {{ form.color_tertiary(class="form-control") }}
        </div>
        <div class="form-group">
            {{ form.web_name.label }}
            {{ form.web_name(class="form-control") }}
        </div>
        <div class="form-group">
            {{ form.logo_url.label }}
            {{ form.logo_url(class="form-control") }}
        </div>
        <button type="submit" class="btn btn-primary">Guardar</button>
    </form>
{% endblock %}
